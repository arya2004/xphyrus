export interface Test {
    title: string;
    description?: string; // Optional
    startDate: Date;
    endDate: Date;
    duration: number; // Duration in minutes or hours depending on your requirement
  }
  